#include <stdio.h>
#include <stdlib.h>

int main(void)
{
    int arr[1000000];
    int i, testCase;

    scanf("%d", &testCase);

    for (i = 0; i < testCase; i++) {
        scanf("%d", &arr[i]);
    }


}